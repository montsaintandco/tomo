# SP3·SP4 v2 Implementation Plan

**Goal:** 남은 사용자 표면(채팅·거래·대행·프로필·마이페이지·로그인·온보딩·판매)과 어드민을 v2 토큰과 사전으로 마감한다 (스펙 `docs/superpowers/specs/2026-09-03-sp3-sp4-v2-design.md`).

### Task 1: i18n 키 +120 (`lib/i18n.ts`) — vitest 통과
### Task 2: SP3 컴포넌트 — `ChatRoom`·`EscrowTimeline`(StepList 공유)·`TxActions`·`ReviewForm`·`ProxyActions`·`LogoutButton` `lang` 프롭
### Task 3: SP3 페이지 — `chat`·`chat/[id]`·`transactions/[id]`·`proxy/[id]`·`profile/[id]`·`mypage`
### Task 4: SP4 폼 — `LoginForm`·`OnboardingForm`·`SellForm` 클라이언트 + 서버 래퍼 페이지(`lang`, `hint`)
### Task 5: SP4 어드민 — `admin`·`center`·`external`·`proxy` + `ExternalItemForm` 토큰
### Task 6: 검증 — tsc·vitest·lint·디텍터, 브라우저(alice 로그인, ko/ja, 375/1440). 문서(DESIGN·HANDOFF) 갱신, 커밋·푸시
