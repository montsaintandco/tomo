# TOMO (토모) — 한일 크로스보더 중고거래 플랫폼 설계

작성일: 2026-07-17 · 상태: 사용자 리뷰 대기

## 1. 개요

한국·일본 각각의 국내 중고거래와 한일 크로스보더 거래를 하나의 플랫폼에서 지원한다.
당근마켓의 신뢰 요소(지역 기반, 신뢰온도)와 메루카리의 안전거래 요소(에스크로 결제,
센터 경유 배송)를 결합한다.

- 거래 범위: KR국내 + JP국내 + KR↔JP 크로스보더 (양방향)
- 형태: 실제 동작하는 서비스 (모바일 우선 반응형 웹)
- 브랜드: 토모 TOMO (友) — 카와이 컨셉

## 2. 브랜드

- 네임: 토모 TOMO / とも. "한일을 잇는 친구"
- 심볼: 파스텔 블루(한국)·파스텔 핑크(일본) 말풍선 두 개가 겹치는 지점에서 하트가 생김
- 앱 아이콘: 투톤 대각 분할 + 흰 하트. 워드마크: TOMO의 마지막 O를 하트로 치환
- 컬러: 토모 블루 `#9CC5EC` / 토모 핑크 `#F2AFAF` / 하트 코랄 `#E2807F` / 배경 아이보리 `#FBF9F4`
- 서체: 한국어 Cafe24 써라운드, 일본어 M PLUS Rounded 1c
- 형태 언어: 큰 라운드(카드 16px, 버튼 풀라운드), 말풍선 모티프를 채팅·배지·툴팁에 반복
- 신뢰온도는 하트 게이지로 표현. 언어 뱃지는 국기 대신 파스텔 원형 KR/JP

## 3. 범위

### MVP 포함

1. 회원가입·온보딩 (국가 KR/JP, 지역 수동 선택, 언어)
2. 상품 등록·피드·검색 (등록 시 상대 언어 번역본 자동 생성·저장)
3. 실시간 채팅 + 자동번역 (발송 시점 번역 저장, 원문 토글)
4. 에스크로 결제 — Stripe 테스트 모드 (국내/크로스보더 상태머신)
5. 크로스보더 센터 물류 — 서울 센터 / 나리타 센터 경유, 입고 확인만 (검수 없음)
6. 센터 운영자 관리 화면 (입고 확인, 국제 발송 처리)
7. 신뢰온도 + 거래 후기
8. 환율 참고 표시 (일 1회 갱신, "¥12,000 (약 108,000원)")

### MVP 제외

찜/팔로우, 가격제안, 푸시 알림, 분쟁 자동화(disputed 상태만 정의), 수령 자동확인 배치,
GPS 동네 인증, 센터 사진·상태 검수, 로컬 PG(토스·PayPay), 커뮤니티 전체.

### 로드맵

- v1.5 — 토모 라운지 1차: "직구 자랑"(거래 완료 데이터가 게시글 초안으로 자동 변환),
  "구해줘 토모"(수배 게시판 → 셀러 응답 시 상품 등록으로 연결). 게시글·댓글 자동번역으로
  양국이 하나의 게시판을 공유.
- v2 — 시세 위키(플랫폼 거래 데이터 기반 KR/JP 시세 카드), 여행 직거래(여행자 매칭으로
  국제배송비 0), 언어교환(데이팅화 리스크 검토 후), 센터 사진 검수, 로컬 PG, 알바·동네업체류
  확장은 거래 기반 확보 후 재평가.

## 4. 아키텍처

- Next.js 14 (App Router) — SEO(상품 검색 유입) + 서버 로직
- Supabase — Postgres, Auth, Realtime(채팅), Storage(이미지), RLS
- Stripe 테스트 모드 — 에스크로 결제
- Claude API — ko↔ja 번역 (상품·채팅)
- Vercel 배포

서버 로직(결제, 번역 호출)은 Next.js Route Handler에 두고 API 키는 서버 전용.
클라이언트 접근 제어는 Supabase RLS.

번역은 쓰기 시점 1회 저장(상품 등록 시, 메시지 발송 시). 조회 시 번역 API 호출 없음.
통화는 원 통화(KRW/JPY)로 저장하고 표시할 때만 환율 테이블로 참고 환산.

## 5. 데이터 모델

| 테이블 | 핵심 컬럼 |
|---|---|
| `profiles` | 국가(KR/JP), 지역(시·구 수동 선택), 언어, 신뢰온도(기본 36.5), admin 롤 |
| `listings` | 판매자, 제목/설명(원문), 원문 언어, 가격+통화, 카테고리, 상태(판매중/예약중/거래완료), 거래방식(직거래/배송/둘다), cross_border_enabled, 이미지 |
| `listing_translations` | listing_id, 언어, 번역 제목/설명 |
| `conversations`, `messages` | 참여자, 메시지 원문+번역문, Realtime 구독 |
| `transactions` | 상태(아래 상태머신), Stripe PaymentIntent ID(유니크), center(SEOUL/NARITA), 국내 운송장, 국제 운송장, 금액 내역(상품가+국제배송비) |
| `exchange_rates` | 통화쌍, 환율, 갱신일 (일 1회, 공개 환율 API에서 갱신 — 실패 시 마지막 값 유지) |
| `reviews` | 거래, 작성자, 별점+한줄 → 신뢰온도 반영 (긍정 +0.1~0.5, 부정 -0.5) |

## 6. 에스크로 상태머신

국내: `pending_payment → paid → shipped → delivered → completed`

크로스보더: `pending_payment → paid → shipped_to_center → center_received → shipped_international → delivered → completed`

공통 이탈: `cancelled`, `disputed` (상태만 정의, 처리 자동화는 제외)

규칙:

- 허용된 전이만 DB 함수에서 검증. 잘못된 전이는 전부 거부
  (예: `shipped_to_center`는 크로스보더만, `shipped`는 국내만)
- 결제 시작 시 상품 `예약중` 전환, 10분 내 미결제 시 해제
- 동시 결제 경합은 DB 조건부 업데이트로 한 명만 성공
- 구매자 7일 내 미확인 시 자동 확인은 설계상 정의만 (MVP는 수동)
- 국제배송비는 구매자가 결제 시 상품가와 함께 결제. 센터까지 국내 배송비는 판매자 부담
- 플랫폼 수수료: 판매가의 10% (메루카리와 동일), `completed` 정산 시 차감
- `completed` 시 정산 (Stripe 테스트 모드 내 처리)

## 7. 핵심 플로우

1. 등록: 사진 → 정보 입력(자국 통화) → 거래방식·크로스보더 허용 선택 → 서버가 번역본 저장
2. 피드: 탭 = 전체 / 내 동네(직거래) / 해외직구. 내 언어로 표시 + 원문 토글, 환산가 표시.
   검색은 번역본 컬럼 포함
3. 채팅: 상대 언어 다르면 번역문 말풍선 + 원문 토글, Supabase Realtime
4. 구매: 상세 → 안전결제(Stripe Checkout) → 상태머신 진행. 크로스보더는 판매자가
   자국 센터로 발송·운송장 입력 → 센터 관리 화면에서 입고 확인·국제 발송 → 구매자 수령 확인
5. 거래 후: 상호 후기 → 신뢰온도 반영

## 8. 페이지 구조

`/` 홈 피드 · `/listings/[id]` 상세 · `/sell` 등록 · `/chat`, `/chat/[id]` 채팅 ·
`/transactions/[id]` 거래 진행(에스크로 타임라인, 운송장, 수령 확인) ·
`/profile/[id]` 프로필(하트 게이지, 상품, 후기) · `/admin/center` 센터 관리 · 온보딩

## 9. 에러 처리

- Stripe webhook 서명 검증, PaymentIntent ID 유니크 제약으로 멱등 처리, 웹훅 재시도로 복구
- 번역 실패 시 등록·발송은 성공, 원문만 저장 후 "번역 준비 중" 표시, 조회 시 재시도
- 전 테이블 RLS: 본인 상품만 수정, 거래 당사자만 거래·채팅 열람, `/admin/center`는 admin 롤만

## 10. 테스트

- 단위: 상태머신 전이(정상 경로 + 불법 전이 거부), 환율 변환·수수료 계산
- 통합: Stripe 테스트 결제 → webhook → 상태 변경
- RLS: 타인 데이터 접근 차단 SQL 레벨 확인
- 수동 E2E: JP 셀러 등록 → KR 구매자 검색·채팅·결제 → 센터 입고·국제발송 → 수령 확인 → 정산 → 후기
